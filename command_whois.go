package main

import (
	"github.com/bwmarrin/discordgo"
)

func (app *App) commandWhoIs(args string, message discordgo.Message, contextual bool) (success bool, err error) {
	// todo
	return true, nil
}
